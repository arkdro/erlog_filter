filter for erlang log files
===========================

Filter items according to include/exclude conditions.

Example:

    filter.py -f crash.log -i http_auth -e starttls -e xmlel

Requirements:

    python3
